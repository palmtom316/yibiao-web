# P3-03 已确认业绩/团队进入技术标

状态：**BLOCKED**。验收日期：2026-09-08～09。负责人：Codex。代码版本以本报告所在 Git 提交为准；尚未生产发布。

实现：business-bid/technical.ts、技术 store/runner 与章节选择 UI。

人工显式选择确认资料和末级章节；结构化金额、证号、人员字段从快照渲染，引用块改写被拒绝。模型不改写权威字段，注入保留人工正文并锁定章节；重新生成/目录保存不能覆盖锁定正文。知识图片引用也复制为项目资产。

验证：business.integration.test.ts 的权威金额变更拒绝、人工正文保护；浏览器项目经理/业绩注入；生命周期图片回归。 统一命令、环境、报告/hash 与运行记录见 [EVIDENCE](EVIDENCE.md)。

边界/未完成项：真实模型叙述质量仍依赖 P0-04/P1-04 的联调；当前权威事实采用确定性渲染。

回退：关闭新的自动注入；保留现有正文和来源，不能清理快照或撤销记录。

前置门禁：本项独立实现与本地验证已经完成，但审核计划指定的上游 P0/业务验收尚未全部放行。本项不绕过前置条件宣布正式完成；待对应真实服务/脱敏样本验收通过后复核放行。
