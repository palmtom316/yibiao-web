# P0-01 验证

环境与起点见 [BASELINE.md](BASELINE.md)。Node 22.23.2 / npm 10.9.3 / pnpm 10.17.1。

- 两端依赖安装成功；补充客户端 tsx、服务端缺失的 markdown-it/adm-zip 类型声明，tsx 纳入运行依赖。
- Prisma generate 成功。后端原有类型错误覆盖 docx 常量/只读选项、转换器声明后缀、Agent 事件/HTTP 类型、查重 Cheerio 节点、目录字段、测试回调类型；均已修复。前端修复模板共享、平台配置保存、知识匹配和全局事实返回类型，补齐目录类型导出，并使用 ES2022 类型库。
- 服务端 `pnpm run typecheck` / `pnpm run build`、客户端 `npm run typecheck` / `npm run build` 均执行通过。客户端保留已有大 chunk 警告，没有为消除警告扩大依赖升级范围。
- 原有后端测试真实执行 144 项（另含主体替换脚本内部 27 条断言），前端 28 项通过。后续新增测试计数见对应任务报告。
- 初次沙箱运行仅报告文件级成功，未收到断言 IPC；该结果作废。测试入口已检查是否收到单项结果，沙箱外复核获得上述真实计数。
- CI 自动发现 TS/MJS/CJS 测试；真实数据库测试单独运行，拒绝非本机、非 `yibiao_test*` 的目标。

回退：可独立恢复测试/工具链入口及类型修正；不涉及业务数据迁移。Node 低于 22.19.0 不在依赖支持范围。

最终回归与容器证据更新至 2026-09-09，见 [EVIDENCE](EVIDENCE.md)：后端 162、前端 28、真实数据库 13 项全部通过；原始基线记录保留供追溯。

工具链/依赖复核补充：宿主早期 npm 10.9.8 结果不作为固定版本证据；最终对齐并重验 10.9.3。运行依赖定向修复及剩余构建依赖适用性见 [DEPENDENCY-AUDIT](DEPENDENCY-AUDIT.md)。
