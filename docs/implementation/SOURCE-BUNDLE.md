# 提交前源码快照

完整源码快照位于 `/tmp/yibiao-verification/yibiao-web-transformation-source.tar.gz`，为首次提交前制作的实施阶段快照；提交版本以 Git 为准。逐文件清单位于同目录 `source-inventory.json`，其自身和源码包 hash 位于 `source-bundle-summary.json`。

该快照不包含 .git、个人 Agent 配置、node_modules、构建目录、真实 .env、证书、运行数据或备份。仓库中的 .env.example、合成测试和构建/部署说明保留。该快照制作时尚未提交；后续源码提交以 Git 历史为准，development 镜像标签不代表生产发布。
